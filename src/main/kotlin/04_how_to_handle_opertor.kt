fun main() {

    // 객체 비교할때, 비교연사자 사용시 compareTo 함수를 자동으로 호출해줌

    /*
        - 동등성(equality) : 두객체의 값이 같은가?
            == 사용하면 간접적으로 equals를 호출해줌
        - 동일성(identity) : 완전히 동일한 객체인가? 주소가 같은가?
            === 사용
     */

    // java와 같이 lazy 연산 수행

    /*
        코들린 내의 특이한 연산자
        - in / !in
            컬렉션이나 범위에 포함/미포함 되어 있다.
        - a..b
            a부터 b까지의 범위 객체를 생성한다.
        - a[i]
            a에서 특정 index i열 값을 가져온다.

     */

}


